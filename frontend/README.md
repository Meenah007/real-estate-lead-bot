# Frontend — React (Vite)

Modern orange & yellow themed customer chat + sales leads view.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Opens at http://localhost:5173  
API is proxied to http://localhost:8000/api/v1

Optional env:

```env
VITE_API_BASE=http://localhost:8000/api/v1
```
